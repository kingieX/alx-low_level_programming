#include <stdio.h>
/**
 * main - function that prints its name, followed by a new line.
 * @argc: argc parameter
 * @argv: argv parameter
 * Return: 0
 */
int main(int argc, char *argv[])
{
	printf("%s\n", *argv);
		return (0);
}
