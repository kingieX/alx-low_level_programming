#include <stdio.h>

/**
 * main - Entry point
 * Return: Always 0 (success)
 */
int main (void)
{
	int a;
	long int b;
	long long int c;
	char d;
	float f;

	printf("size of a char: %la byte(s)\n", (unsigned long)sizeof(d);
	printf("size of a int: %la byte(s)\n", (unsigned long)sizeof(a);
	printf("size of a long int: %la byte(s)\n", (unsigned long)sizeof(b);
	printf("size of long long int: %la byte(s)\n", (unsigned long long)sizeof(c);
	printf("size of float: %la byte(0)\n" (unsigned float)sizeof(f);
	return(0);
}
	
